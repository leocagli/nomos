// Marketplace home — Gamified village map interface
// Each building in the village represents a different function in the app
import { VillageMap } from "@/components/VillageMap";

export const dynamic = "force-dynamic";

export default function MarketplacePage() {
  return <VillageMap />;
}
